import { PROJECT_NAME } from "@/configs/env";

export const SKELETON_TYPES = {
  BARS: "bars",
};

export const LOCAL_STORAGE_KEYS = {
  AUTH_DATA: `${PROJECT_NAME}-auth-data`,
  ACTIVITY_SESSION: `${PROJECT_NAME}-activity-session`,
};

export const COMPANY_CODES = "Ig";
